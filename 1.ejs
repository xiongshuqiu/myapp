<!-- 在过滤器区域添加 "Edit" 链接 -->
<% linkItems.forEach(function(item) { %> <% if (item.visible) { %>
<a
  href="/users/<%= user._id %>/update"
  class="editLink"
  style="color: blue; display: inline"
  >Edit</a
>
<% } %> <% }); %>
