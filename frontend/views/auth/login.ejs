<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Login</title>
    <link rel="stylesheet" type="text/css" href="/stylesheets/style.css" />
    <style>
      body,
      html {
        height: 100%;
        margin: 0;
        background-image: url('/images/login/image2.jpg'); /* Set background image */
        background-size: cover; /* Ensure image covers entire background */
        background-position: center; /* Center the background image */
        font-family: Arial, sans-serif;
        display: flex;
        justify-content: flex-end; /* Align form to the right */
        align-items: center; /* Vertically center */
      }
      .container {
        display: flex;
        width: 400px; /* Set container width */
        height: auto; /* Height adjusts based on content */
        padding: 20px;
        background-color: rgba(255, 255, 255, 0.8); /* White background with transparency */
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Add shadow effect */
        border-radius: 15px; /* Add rounded corners */
        margin-right: 150px; /* Leave space on the right */
        margin-top: 10px; /* Move container up */
      }
      .login-form {
        width: 100%;
        padding: 20px;
        background-color: white; /* Keep form background white */
        border-radius: 10px; /* Add rounded corners */
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Add shadow effect */
        border: 1px solid #ccc; /* Add border */
      }
      .login-form h1 {
        text-align: center;
        margin-top: 0; /* Remove top margin */
        margin-bottom: 40px; /* Add more bottom margin */
        font-size: 24px; /* Reduce font size */
        font-weight: normal; /* Use normal font weight */
      }
      .login-form label,
      .login-form input,
      .login-form button {
        display: block;
        width: calc(100% - 40px); /* Adjust width to be the same for all */
        margin: 10px 20px; /* Adjust margins */
      }
      .login-form input,
      .login-form button {
        padding: 10px;
        border: 1px solid #ccc;
        border-radius: 4px; /* Add rounded corners */
      }
      .login-form button {
        background-color: #4682b4; /* Set button background color to steel blue */
        color: white;
        cursor: pointer;
        transition: background-color 0.3s ease;
        width: 95%; /* Make button width 100% of the container */
        margin-top: 20px; /* Add more space above the button */
      }
      .login-form button:hover {
        background-color: #357ab9; /* Set button hover background color to darker steel blue */
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="login-form">
        <h1>Elderly Care Management System Login</h1>
        <% if (typeof err !== 'undefined' && err) { %>
        <div class="error-message"><%= message %></div>
        <% } %>
        <form action="/auth/login" method="post" id="login-form">
          <label for="userName">Username:</label>
          <input type="text" id="userName" name="userName" required />
          <label for="passWord">Password:</label>
          <input type="password" id="passWord" name="passWord" required />
          <button type="submit">Login</button>
        </form>
      </div>
    </div>
  </body>
</html>
