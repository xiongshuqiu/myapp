<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Update BedStatus Information</title>
    <!-- 1.框架样式：Bootstrap Styles-->
    <link href="/assets/css/bootstrap.css" rel="stylesheet" />
    <!-- 2.图标样式：FontAwesome Styles-->
    <link href="/assets/css/font-awesome.css" rel="stylesheet" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
    />
    <!--3.普通样式：Custom Styles-->
    <link href="/assets/css/custom-styles.css" rel="stylesheet" />
    <!-- 4.字体样式：Google Fonts-->
    <link
      href="http://fonts.useso.com/css?family=Open+Sans"
      rel="stylesheet"
      type="text/css"
    />
  </head>

  <body>
    <div id="wrapper">
       <!-- 顶部导航条 -->
       <%- include('../../block/top-navbar.ejs') %>
       <!-- 左侧导航条 -->
       <%- include('../../block/navbar-side.ejs') %>

      <div id="page-wrapper">
        <div class="header">
          <h1 class="page-header">
            Update BedStatus Information <small>Modify the user's details.</small>
          </h1>
          <ol class="breadcrumb">
            <li><a href="#">Home</a></li>
            <li><a href="#">Forms</a></li>
            <li class="active">Update BedStatus</li>
          </ol>
        </div>

        <div id="page-inner">
          <div class="row">
            <div class="col-lg-12">
              <div class="panel panel-default">
                <div class="panel-heading">BedStatus Information</div>
                <div class="panel-body">
                  <div class="row">
                    <div class="col-lg-6">
                      <form
                        action="/beds/status/<%= bedStatus._id %>?_method=PUT"
                        method="post"
                        role="form"
                      >
                      <div class="form-group">
                        <label for="building">Building <span style="color: red">*</span></label>
                        <select class="form-control" id="building" name="building" required>
                          <option value="1" <%= bedStatus.building == 1 ? 'selected' : '' %>>1</option>
                          <option value="2" <%= bedStatus.building == 2 ? 'selected' : '' %>>2</option>
                          <option value="3" <%= bedStatus.building == 3 ? 'selected' : '' %>>3</option>
                        </select>
                      </div>
                      
                      <div class="form-group">
                        <label for="floor">Floor <span style="color: red">*</span></label>
                        <select class="form-control" id="floor" name="floor" required>
                          <option value="1" <%= bedStatus.floor == 1 ? 'selected' : '' %>>1</option>
                          <option value="2" <%= bedStatus.floor == 2 ? 'selected' : '' %>>2</option>
                          <option value="3" <%= bedStatus.floor == 3 ? 'selected' : '' %>>3</option>
                          <option value="4" <%= bedStatus.floor == 4 ? 'selected' : '' %>>4</option>
                        </select>
                      </div>
                      
                      <div class="form-group">
                        <label for="room">Room <span style="color: red">*</span></label>
                        <input
                          type="number"
                          class="form-control"
                          id="room"
                          name="room"
                          value="<%= bedStatus.room %>"
                          placeholder="Enter Room Number (e.g., 101)"
                          required
                          pattern="^\d{3}$"
                          title="Room format should be Room Number (e.g., 101)"
                        />
                        <small class="form-text text-muted">
                          Format: Room Number (e.g., 101)
                        </small>
                      </div>
                      
                      <div class="form-group">
                        <label for="roomType">Room Type <span style="color: red">*</span></label>
                        <select class="form-control" id="roomType" name="roomType" required>
                          <option value="s" <%= bedStatus.roomType === 's' ? 'selected' : '' %>>s</option>
                          <option value="b" <%= bedStatus.roomType === 'b' ? 'selected' : '' %>>b</option>
                        </select>
                        <small class="form-text text-muted">
                          Select 's' or 'b' (e.g., 701s, 701b)
                        </small>
                      </div>
                      
                      <div class="form-group">
                        <label for="bedNumber">Bed Number <span style="color: red">*</span></label>
                        <select class="form-control" id="bedNumber" name="bedNumber" required>
                          <option value="01" <%= bedStatus.bedNumber === '01' ? 'selected' : '' %>>01</option>
                          <option value="02" <%= bedStatus.bedNumber === '02' ? 'selected' : '' %>>02</option>
                          <option value="03" <%= bedStatus.bedNumber === '03' ? 'selected' : '' %>>03</option>
                          <option value="04" <%= bedStatus.bedNumber === '04' ? 'selected' : '' %>>04</option>
                          <option value="05" <%= bedStatus.bedNumber === '05' ? 'selected' : '' %>>05</option>
                        </select>
                      </div>
                      
                      <div class="form-group">
                        <label for="bedId">Bed ID <span style="color: red">*</span></label>
                        <input
                          type="text"
                          class="form-control"
                          id="bedId"
                          name="bedId"
                          placeholder="Generated Bed ID will appear here"
                          required
                          readonly
                        />
                        <small class="form-text text-muted">
                          Format: Building-Floor-Room Room Type-Bed Number (e.g., 3-7-701s-01)
                        </small>
                      </div>
                      
                      <button type="button" class="btn btn-primary" onclick="generateBedId()">Generate Bed ID</button>
                      
                      <div class="form-group">
                        <label for="status">Status <span style="color: red">*</span></label>
                        <select class="form-control" id="status" name="status" required>
                          <option value="occupied" <%= bedStatus.status === 'occupied' ? 'selected' : '' %>>occupied</option>
                          <option value="available" <%= bedStatus.status === 'available' ? 'selected' : '' %>>available</option>
                          <option value="reserved" <%= bedStatus.status === 'reserved' ? 'selected' : '' %>>reserved</option>
                          <option value="maintenance" <%= bedStatus.status === 'maintenance' ? 'selected' : '' %>>maintenance</option>
                        </select>
                      </div>
                      
                      <script>
                        function generateBedId() {
                          const building = document.getElementById('building').value;
                          const floor = document.getElementById('floor').value;
                          const room = document.getElementById('room').value;
                          const roomType = document.getElementById('roomType').value;
                          const bedNumber = document.getElementById('bedNumber').value;
                          if (building && floor && room && bedNumber) {
                            const fullRoom = roomType ? room + roomType : room; // 添加后缀
                            const bedId = `${building}-${floor}-${fullRoom}-${bedNumber}`;
                            document.getElementById('bedId').value = bedId;
                          } else {
                            alert('Please fill in all fields to generate the Bed ID.');
                          }
                        }
                      
                        // 页面加载时自动生成 Bed ID
                        window.onload = function() {
                          generateBedId();
                        }
                      </script>
                      
                      

                        <button type="submit" class="btn btn-primary">
                          Save Changes
                        </button>
                        <button
                          type="reset"
                          class="btn btn-secondary"
                          onclick="window.location.href='/beds/status/';"
                        >
                          Return
                        </button>
                      </form>
                      <div id="message">
                        <% if (typeof message !== 'undefined' && message !==
                        null) { %>
                        <div class="alert alert-success"><%= message %></div>
                        <% } %>
                      </div>
                    </div>
                  </div>
                </div>
                <!-- /.panel-body -->
              </div>
              <!-- /.panel -->
            </div>
            <!-- /.col-lg-12 -->
          </div>
        </div>
        <!-- /. PAGE INNER  -->
      </div>
      <!-- /. PAGE WRAPPER  -->
    </div>
    <!-- /. WRAPPER  -->
    <!-- JS Scripts-->
    <!-- jQuery Js -->
    <script src="/assets/js/jquery-1.10.2.js"></script>
    <!-- Bootstrap Js -->
    <script src="/assets/js/bootstrap.min.js"></script>
    <!-- Metis Menu Js -->
    <script src="/assets/js/jquery.metisMenu.js"></script>
    <!-- Custom Js -->
    <script src="/assets/js/custom-scripts.js"></script>
  </body>
</html>
