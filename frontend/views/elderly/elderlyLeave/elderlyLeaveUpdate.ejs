<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Update BedAssignment Information</title>
    <!-- 1.框架样式：Bootstrap Styles-->
    <link href="/assets/css/bootstrap.css" rel="stylesheet" />
    <!-- 2.图标样式：FontAwesome Styles-->
    <link href="/assets/css/font-awesome.css" rel="stylesheet" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
    />
    <!--3.普通样式：Custom Styles-->
    <link href="/assets/css/custom-styles.css" rel="stylesheet" />
    <!-- 4.字体样式：Google Fonts-->
    <link
      href="http://fonts.useso.com/css?family=Open+Sans"
      rel="stylesheet"
      type="text/css"
    />
  </head>

  <body>
    <div id="wrapper">
     <!-- 顶部导航条 -->
     <%- include('../../block/top-navbar.ejs') %>
     <!-- 左侧导航条 -->
     <%- include('../../block/navbar-side.ejs') %>

      <div id="page-wrapper">
        <div class="header">
          <h1 class="page-header">
            Update EmployeeShiftSchedule Information <small>Modify the EmployeeShiftSchedule's details.</small>
          </h1>
          <ol class="breadcrumb">
            <li><a href="#">Home</a></li>
            <li><a href="#">Forms</a></li>
            <li class="active">Update EmployeeShiftSchedule</li>
          </ol>
        </div>

        <div id="page-inner">
          <div class="row">
            <div class="col-lg-12">
              <div class="panel panel-default">
                <div class="panel-heading">EmployeeShiftSchedule Information</div>
                <div class="panel-body">
                  <div class="row">
                    <div class="col-lg-6">
                      <form
                        action="/employees/shiftSchedule/<%= employeeShiftSchedule._id  %>?_method=PUT"
                        method="post"
                        role="form"
                      >
                  
                      <div class="form-group">
                        <label for="shiftScheduleId">Shift Schedule ID <span style="color: red">*</span></label>
                        <input
                          type="text"
                          class="form-control"
                          id="shiftScheduleId"
                          name="shiftScheduleId"
                          value="<%= employeeShiftSchedule.shiftScheduleId %>"
                          required
                          pattern="SS\d{4}"
                          title="The Shift Schedule ID should follow the format: two capital letter followed by four digits (e.g., SS0001)."
                        />
                        <small class="form-text text-muted">
                          The Shift Schedule ID  should follow the format: two capital letter followed by four digits (e.g., SS0001).
                        </small>
                      </div>

                      <div class="form-group">
                        <label for="employeeId">Employee ID(Name) <span style="color: red">*</span></label>
                        <select class="form-control" id="employeeId" name="employeeId" required>
                          <% if (employeeIds && employeeIds.length > 0) { %>
                            <% employeeIds.forEach(function(employee) { %>
                              <option value="<%= employee.employeeId %>" <%= employeeShiftSchedule.employeeId === employee.employeeId ? 'selected' : '' %>>
                                employeeId: <%= employee.employeeId %> (<%= employee.employeeName %>)
                              </option>
                            <% }); %>
                          <% } else { %>
                            <option value="">No existing employeeId</option>
                          <% } %>
                        </select>
                      </div>
                      
                    
                      <div class="form-group">
                        <label for="shiftType"> Shift Type <span style="color: red">*</span></label>
                        <select class="form-control" id="shiftType" name="shiftType" required>
            
                              <option value="Morning" <%= employeeShiftSchedule.shiftType === 'Morning' ? 'selected' : '' %>>
                                Morning
                              </option>
                              <option value="Evening" <%= employeeShiftSchedule.shiftType === 'Evening' ? 'selected' : '' %>>
                                Evening
                              </option>
                              <option value="Night" <%= employeeShiftSchedule.shiftType === 'Night' ? 'selected' : '' %>>
                                Night
                              </option>
                        </select>
                      </div>
                                     
                        <div class="form-group">
                          <label for="startTime">Start Time <span style="color: red">*</span></label>
                          <div class="input-wrapper">
                            <input
                              type="date"
                              class="form-control date-input"
                              id="startTime"
                              name="startTime"
                              value="<%= employeeShiftSchedule.startTime ? new Date(employeeShiftSchedule.startTime).toISOString().split('T')[0] : '' %>"
                               />
                            </div>
                          </div>
                       
                        <div class="form-group">
                          <label for="endTime">End Time </label>
                          <div class="input-wrapper">
                            <input
                              type="date"
                              class="form-control date-input"
                              id="endTime"
                              name="endTime"
                              value="<%= employeeShiftSchedule.endTime ? new Date(employeeShiftSchedule.endTime).toISOString().split('T')[0] : '' %>" 
                              />
                          </div>
                        </div>
                        
                        <button type="submit" class="btn btn-primary">
                          Save Changes
                        </button>
                        <button
                          type="reset"
                          class="btn btn-secondary"
                          onclick="window.location.href='/employees/shiftSchedule/';"
                        >
                          Return
                        </button>
                      </form>
                      <div id="message">
                        <% if (typeof message !== 'undefined' && message !==
                        null) { %>
                        <div class="alert alert-success"><%= message %></div>
                        <% } %>
                      </div>
                    </div>
                  </div>
                </div>
                <!-- /.panel-body -->
              </div>
              <!-- /.panel -->
            </div>
            <!-- /.col-lg-12 -->
          </div>
        </div>
        <!-- /. PAGE INNER  -->
      </div>
      <!-- /. PAGE WRAPPER  -->
    </div>
    <!-- /. WRAPPER  -->
    <!-- JS Scripts-->
    <!-- jQuery Js -->
    <script src="/assets/js/jquery-1.10.2.js"></script>
    <!-- Bootstrap Js -->
    <script src="/assets/js/bootstrap.min.js"></script>
    <!-- Metis Menu Js -->
    <script src="/assets/js/jquery.metisMenu.js"></script>
    <!-- Custom Js -->
    <script src="/assets/js/custom-scripts.js"></script>
  </body>
</html>
